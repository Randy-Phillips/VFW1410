var myText = ["hello", "world", "last"];



var fieldText = Ti.UI.createLabel({
	color:"black",
	text: myText[0] ,
	font:{fontSize:24,fontFamily:"Helvetica Neue"},
	textAlign: "center",
	width: "auto"
});

var enterText = function() {
	var i = fieldText.text
	var indexNum = myText.indexOf(i);
	var textLength = myText.length
	if ( i !== myText[textLength] ) {
		indexNum = indexNum + 1;
		fieldText.text = myText[indexNum];
	};
	
	
	(var i=0; i < myText.length; i++ ) {
	}



textField.add(fieldText);
